<form method="post" action="{{ path('app_car_model_delete', {'id': car_model.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_model.id) }}">
    <button class="btn">Delete</button>
</form>
